import * as React from 'react';
import Uploader from '../../components/common/Uploader/Uploader';

interface IUploadVideoPageProps {
}

const UploadVideoPage: React.FunctionComponent<IUploadVideoPageProps> = (props) => {
  return <Uploader />;
};

export default UploadVideoPage;
